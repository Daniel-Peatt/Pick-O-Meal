<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Random Meal</title>
    <!-- Bootstrap link -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Local CSS -->
    <link rel="stylesheet" href="/styles/randomMeal.css">
</head>
<!-- Animated Background -->
<div class="bg"></div>
<div class="bg bg2"></div>
<div class="bg bg3"></div>

<body class="container">
    <!-- Header -->
     <%- include('partial/header.ejs') %>
    <div class="row m-5">
        <!-- Left Column -->
        <div class="col-12 col-lg-6">
            <!-- Meal Title -->
            <div class="row box mealName">
                <h1><%= meals.strMeal %></h1>
            </div>
            <!-- Category -->
            <div class="row box mealInfo pt-2">
                <h5>Category: <%= meals.strCategory %> | Origin: <%= meals.strArea %></h5>
                <h3>Directions:</h3><br>
            </div>
            <!-- Instructions -->
            <div class="row box instruction">
                <ul>
                    <% for (let n = 0; n < instructions.length; n++) { %>
                    <li><%= instructions[n] %></li><br>
                    <% } %>
                </ul>   
            </div>
            <!-- Measurements -->
            <div class="row box measurements">
                <h3>Ingredients:</h3><br>
                <ul>
                    <% for(let i = 0; i < ingredients.length; i++) { %>
                    <li><%= ingredients[i] %> | <%= measurements[i] %></li>
                    <% } %>
                </ul>
            </div>
        </div>
        <!-- Right Column -->
        <div class="col box rCol">
            <!-- Meal Photo -->
            <div class="row box">
                <img src="<%= meals.strMealThumb %>" alt="Photo of suggested meal" class="pt-3 pb-3 meal-img">
            </div>
            <!-- Youtube player -->
            <div class="row embed-responsive embed-responsive-16by9 pt-3">
                <iframe class="embed-responsive-item" src=<%= youTubeLink %> allowfullscreen></iframe>
            </div>
        </div>
    </div>
    <!-- Bootstrap script -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>